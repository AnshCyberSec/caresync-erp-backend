spring:
  application:
    name: CareSync-ERP

  datasource:
    url: jdbc:mysql://localhost:3306/caresync_db
    username: root
    password: anshuman@123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

  mail:
    host: smtp.gmail.com
    port: 587
    username: anshumananshuman63@gmail.com
    password: rnqamgkzlhfsqfpk
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true

logging:
  level:
    root: INFO
    com.caresync.erp: DEBUG
    org.springframework.web: INFO
    org.hibernate.SQL: WARN

  file:
    name: logs/hospital-app.log

  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"

app:
  otp:
    expiry-minutes: 5